name: Keep Render Awake

on:
  schedule:
    - cron: '* * * * *'
  workflow_dispatch: {}

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Traketeros
        run: curl -sf https://traketeros.onrender.com/ || echo "Fallo el ping, pero seguimos"
